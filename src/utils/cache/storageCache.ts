// import { isDevEnv } from '../getEnv';

// class WebStorage {
//     private storage: Storage;
//     private hasEncrypt: boolean;
//     private KEY: string;
//     private expire: null | number;

//     constructor({storage = sessionStorage, hasEncrypt = true, key ="", expire = null}) {
//         this.storage = storage;
//         this.hasEncrypt = isDevEnv() ? false : hasEncrypt;
//         this.KEY = key;
//         this.expire = expire;
//     }

//     get(key?: string) {

//     }

//     set(value: string | number | object | [] | null, objectKey?: any) {

//     }
// }
